name: 1. VirtualBox Matrix (69 OSes)
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu, debian, fedora, arch, kali]
    steps:
      - name: Testing VirtualBox Intent for ${{ matrix.os }}
        run: echo "Testing VirtualBox installation for ${{ matrix.os }}..."
